<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cascv.oas.server.blockchain.mapper.EthNetworkMapper">

  <resultMap id="ethNetworkMap" type="com.cascv.oas.server.blockchain.model.EthNetworkModel">
	<id column="name" property="name" />
	<result column="provider" property="provider"/>
	<result column="description" property="description"/>
  </resultMap>
  
  <sql id="Base_Column_List">
    name, provider, description
  </sql>

  
  <!-- insert selective -->
  <insert id="insertSelective" parameterType="com.cascv.oas.server.blockchain.model.EthNetworkModel">
    insert into eth_network
      <trim prefix="(" suffix=")" suffixOverrides=",">
          name,
          provider,
        
        <if test="description != null">
          description,
        </if>
      </trim>
      <trim prefix="values (" suffix=")" suffixOverrides=",">
        #{name,jdbcType=VARCHAR},
        #{provider,jdbcType=VARCHAR},
        <if test="description != null">
  		    #{description,jdbcType=VARCHAR},
        </if>
      </trim>
  </insert>
 
  <select id="selectByName" parameterType="java.lang.String" resultMap="ethNetworkMap">
  	select 
      <include refid="Base_Column_List" />
    from eth_network where name = #{name}
  </select>

  <select id="selectAll" resultMap="ethNetworkMap">
  	select 
      <include refid="Base_Column_List" />
    from eth_network
  </select>
  
  <!-- delete by name -->
  <delete id="deleteByName">
    delete from eth_network where name = #{name}
  </delete>


</mapper>